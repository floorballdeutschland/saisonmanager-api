<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style>
      /* Email styles need to be inline */
    </style>
  </head>

  <body>
    <%= yield %>
    <%# Datenschutzinformation nur an die betroffene Person; das Flag setzen die
        Mailer-Aktionen (siehe TransferRequestMailer#audience_recipients). Im Layout,
        damit ein gepflegter EmailTemplate-Body sie nicht mit entfernt. %>
    <%= render 'layouts/privacy_notice' if @privacy_notice %>
  </body>
</html>
